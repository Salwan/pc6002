; General Utilities
IFNDEF __UTILS__
DEFINE __UTILS__

; Memory copy
; source = HL
; destination = DE
; size in bytes = BC
; This is just LDDR, I'm not sure I can do faster?
;MEMCPY:
;	LDDR
;	RET


ENDIF