> Install Python 2.7+ (Python 3 might work too but I haven't tested it)
> Install pillow using command line: pip install pillow

If "python" and/or "pip" commands are not found, you just need to add paths to: Pyhon27/ and Python27/Scripts to your environment path variable.
